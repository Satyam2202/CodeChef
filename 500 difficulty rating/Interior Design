#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int t;
	cin>>t;
	while(t--){
	    int x1,y1,x2,y2;
	    cin>>x1>>y1>>x2>>y2;
	    x1=x1+y1;
	    x2=x2+y2;
	    if(x1>x2){
	        cout<<x2<<endl;
	    }
	    else{
	        cout<<x1<<endl;
	    }
	}

}
